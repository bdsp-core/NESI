# Python environment for the NESI (Neurophysiologic Encephalopathy Severity Index) pipeline.
# Tested with Python 3.9. Install with:  pip install -r requirements.txt
#
# Versions are lower bounds known to work. For an exact, reproducible environment,
# freeze the authors' environment with `pip freeze > requirements.lock.txt`.

# --- core scientific stack ---
numpy>=1.23
pandas>=1.5
scipy>=1.9
scikit-learn>=1.1
statsmodels>=0.13        # mixed-effects variance decomposition (Table 3)
matplotlib>=3.6
seaborn>=0.12

# --- deep learning ---
torch>=1.13
torchsummary>=1.5
coral-pytorch>=1.4       # CORAL/CORN ordinal utilities used by the ResNet encoder

# --- EEG / signal processing ---
mne>=1.3

# --- I/O for EEG feature matrices and checkpoints ---
h5py>=3.7
hdf5storage>=0.1.19
mat73>=0.60

# --- analysis utilities ---
tqdm>=4.64
joblib>=1.2
duckdb>=0.8              # fast querying of medication / flowsheet tables
lifelines>=0.27         # survival / mortality analysis
shap>=0.41              # feature attribution
pacmap>=0.7             # supplementary embedding atlases
openpyxl>=3.0           # reading the .xlsx metadata
python-docx>=0.8.11     # Table 1 / table export to .docx
